<!--pages/upload/status/status.wxml-->
<view class="container">
  <!-- 进度展示 -->
  <view class="status-section" wx:if="{{status !== 'completed'}}">
    <view class="status-icon">
      <view class="pulse-circle"></view>
    </view>
    <text class="status-title">{{statusText}}</text>
    <text class="status-desc">{{statusDesc}}</text>
    
    <!-- 进度条 -->
    <view class="progress-bar">
      <view class="progress-track">
        <view class="progress-fill" style="width: {{progress}}%"></view>
      </view>
      <text class="progress-text">{{progress}}%</text>
    </view>
    
    <!-- 步骤状态 -->
    <view class="steps-list">
      <view class="step-item {{item.completed ? 'completed' : ''}} {{item.active ? 'active' : ''}}" wx:for="{{steps}}" wx:key="name">
        <view class="step-icon">
          <icon wx:if="{{item.completed}}" name="check-circle" color="mint" size="36" />
          <icon wx:elif="{{item.active}}" name="alert-circle" color="brand" size="36" />
          <icon wx:else name="alert-circle" color="text-muted" size="36" />
        </view>
        <text class="step-name">{{item.name}}</text>
      </view>
    </view>
  </view>

  <!-- 解析完成 -->
  <view class="result-section" wx:if="{{status === 'completed'}}">
    <view class="success-header">
      <view class="success-icon"><icon name="check" color="white" size="56" strokeWidth="3" /></view>
      <text class="success-title">好了</text>
      <text class="success-desc">以下是我们从病历里看到的信息，哪里不对直接改。</text>
    </view>

    <!-- 病历关键信息 -->
    <view class="result-card">
      <view class="card-title">病历信息</view>
      <view class="info-list">
        <view class="info-item" wx:for="{{parsedResult}}" wx:key="label">
          <text class="info-label">{{item.label}}</text>
          <text class="info-value">{{item.value}}</text>
        </view>
      </view>
    </view>

    <!-- 操作按钮 -->
    <view class="action-btns">
      <button class="btn btn-default" bindtap="editResult">手动修正</button>
      <button class="btn btn-primary" bindtap="goToMatches">查看找到的新药</button>
    </view>
  </view>

  <!-- 错误状态 -->
  <view class="error-section" wx:if="{{status === 'error'}}">
    <view class="error-icon"><icon name="x-circle" color="white" size="56" strokeWidth="3" /></view>
    <text class="error-title">解析失败</text>
    <text class="error-desc">{{errorMsg}}</text>
    <button class="btn btn-primary" bindtap="retry">重新上传</button>
  </view>
</view>